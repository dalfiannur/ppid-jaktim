<div class="w-fit p-4 flex items-center gap-6">
    <img src="{{ $icon }}" class="w-[86px] aspect-square">
    <div class="flex flex-col gap-2 text-center">
        <h1 class="text-4xl leading-10 font-extrabold">{{ $value }}</h1>
        <p class="text-2xl leading-7 font-medium text-gray-500">{{ $title }}</p>
    </div>
</div>
